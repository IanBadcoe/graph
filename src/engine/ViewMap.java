package engine;

import java.util.ArrayList;

class ViewMap
{
//   engine.ViewMap createFromEye(engine.XY eye_pos, engine.Level level)
//   {
//      double northern_edge = level.getBounds().Max.Y;
//
//      engine.XY start_probe = new engine.XY(0, northern_edge);
//      double angle = 0;
//
//      engine.Wall curr_wall = level.nearestWall(eye_pos, start_probe);
//
//   }

   private final ArrayList<OrderedPair<Double, Double>> m_map = new ArrayList<>();
}
